{% extends 'base.html.twig' %}

{% block title %}Éditer adresse{% endblock %}
{% block body %}

	{{ form_start(form) }}

        {{ form_row(form.street) }}
        {{ form_row(form.zipCode) }}
        {{ form_row(form.city) }}

        <button type="submit" class="btn btn-primary">Créer</button>
	{{ form_end(form) }}

{% endblock %}